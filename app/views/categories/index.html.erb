<h2> Categories: <%= @categories.length %>  </h2>
<ol>
    <% @categories.each do |category|%>
    <li>
        <%= category.title %>
        (<%= category.product_count %> products)

    </li>
    <%end%>
</ol>
